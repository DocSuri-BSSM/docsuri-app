# RN 네이티브 오토링킹은 flat(hoisted) node_modules를 전제로 동작한다.
# pnpm 11부터 이 설정은 .npmrc가 아니라 이 파일에서 읽힌다. 절대 지우지 말 것.
nodeLinker: hoisted

allowBuilds:
  # eslint-import-resolver의 네이티브 바인딩. lint 성능용.
  unrs-resolver: true
