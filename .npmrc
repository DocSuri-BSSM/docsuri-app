# React Native 네이티브 오토링킹은 flat(hoisted) node_modules를 전제로 동작한다.
# 이 값을 지우면 autolinking이 모듈을 찾지 못해 빌드가 깨진다. 절대 삭제 금지.
# ※ pnpm 11부터는 이 설정을 pnpm-workspace.yaml의 `nodeLinker: hoisted`에서 읽는다.
#    두 파일을 항상 같이 유지할 것.
node-linker=hoisted
